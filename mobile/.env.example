# API base URL — must include /api/v1 (same contract as web NEXT_PUBLIC_API_URL).
# Use your machine's LAN IP for Expo Go on a phone (not localhost).
# API default port is 8080 (see api/.env PORT).
EXPO_PUBLIC_API_URL=http://192.168.1.x:8080/api/v1
